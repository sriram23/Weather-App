<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ProgressStyle" parent="@style/Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateDrawable">@drawable/spin_animation</item>
        <!--<item name="android:indeterminateDrawable" tools:ignore="DuplicateDefinition">@drawable/sunset</item>-->
        <item name="android:minHeight">50dp</item>
        <item name="android:minWidth">50dp</item>
    </style>

</resources>
